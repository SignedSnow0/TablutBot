# README

Either clone with
```bash
git clone --recursive <url>
```
or pull submodules 
```bash
git submodule init && git submodule update
```
and then run 
```bash
make lsp
```
to generate code completion files
